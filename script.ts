interface Math {
    /**
     * Converts an angle into a radian
     * @param angle The angle to convert
     */
    radian(angle: number): number
    /**
     * Converts an radian into a angle
     * @param radian The radian to convert
     */
    angle(radian: number): number
}